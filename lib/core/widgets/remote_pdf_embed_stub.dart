import 'package:flutter/widgets.dart';

class RemotePdfEmbed extends StatelessWidget {
  const RemotePdfEmbed({
    required this.url,
    super.key,
  });

  final String url;

  @override
  Widget build(BuildContext context) {
    return const SizedBox.shrink();
  }
}
